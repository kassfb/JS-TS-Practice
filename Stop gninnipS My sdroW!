https://www.codewars.com/kata/5264d2b162488dc400000001/javascript

Title:
Stop gninnipS My sdroW!


Description:
Write a function that takes in a string of one or more words, and returns the same string, but with all words that have five or more letters reversed (Just like the name of this Kata). Strings passed in will consist of only letters and spaces. Spaces will be included only when more than one word is present.

Examples:

"Hey fellow warriors"  --> "Hey wollef sroirraw" 
"This is a test        --> "This is a test" 
"This is another test" --> "This is rehtona test"

//1 try.
function spinWords(string){
  const words = string.trim().split(' ');
  let resStr = '';
  
  words.forEach(word => {
    if (word.length >= 5) {
      const letters = word.split('');
      letters.reverse();
      
      resStr += ' ' + letters.join('');
      return;
    }
    resStr += ' ' + word;
  })
  
  return resStr.trim();
}

// Clean.
function spinWords(string){
  const arrayWords = string.split(" ");
  if (arrayWords.length === 1) {
    return shouldRevert(string)
           ? revertWord(string)
           : string;
  }
  else {
    let spinnedArray = arrayWords.map(word => {
      return shouldRevert(word)
             ? revertWord(word)
             : word
    })
    
    return spinnedArray.join(" ")
  }
}

function shouldRevert(word) {
  return word.length >= 5
}

function revertWord(word){
  if (word.length) {
    return word.split("").reverse().join("")
  }
}

console.log(spinWords("Hi!"));
console.log(spinWords("Hey fellow warriors"));
console.log(spinWords("This is a test"));
console.log(spinWords("This is another test"));
console.log(spinWords("Just kidding there is still one more"));
